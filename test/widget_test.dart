import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:team_run_challenge/main.dart';

void main() {
  test('TeamChallengeApp is a stateless root widget', () {
    expect(const TeamChallengeApp(), isA<StatelessWidget>());
  });
}
